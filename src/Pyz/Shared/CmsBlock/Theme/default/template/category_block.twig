{% block content %}
    <!-- CMS_BLOCK_PLACEHOLDER : "title" -->
    <!-- CMS_BLOCK_PLACEHOLDER : "description" -->
    <!-- CMS_BLOCK_PLACEHOLDER : "testSection" -->
    <div class="catalog-cms-block__inner">
        {% if spyCmsBlockPlaceholder('title') %}
            <h5>{{ spyCmsBlockPlaceholder('title') | raw }}</h5>
        {% endif %}
        {{ spyCmsBlockPlaceholder('description') | raw }}
        {# testCode CMS content-item block extra field #}
        {{ spyCmsBlockPlaceholder('testSection') | raw }}
    </div>
{% endblock %}

